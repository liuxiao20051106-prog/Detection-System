# 安全说明

## 支持范围

安全修复仅针对默认分支的最新版本。报告漏洞时，请使用 GitHub 仓库的 **Security > Report a vulnerability** 私密报告功能，不要在公开 Issue 中附上可利用细节、敏感文件或未公开模型。

报告应包含：受影响版本/提交、复现步骤、影响、建议修复和可公开时间。维护者将在 GitHub 可用的前提下通过私密报告协调。

## 模型和媒体信任边界

- PyTorch `.pt` 权重可包含序列化对象。只加载信任来源的权重。
- 程序启动和每个工作线程都会核对 SHA-256；哈希只检测文件变化，不能证明发布者可信。
- 图片/视频会检查扩展名、文件大小、解码结果、像素数和视频时长，但解码器仍属外部攻击面。
- 在与不可信文件交互时，建议使用权限受限的操作系统账户和隔离环境。

## 输出安全

- 结果保存到用户数据目录，不写入安装目录。
- 同名结果使用递增后缀，不覆盖旧文件。
- CSV 字段会中和常见表格公式前缀。
- 视频先写入临时文件，释放编码器并重新打开验证后才原子替换为最终文件；取消会删除部分输出。

## 依赖管理

运行依赖精确锁定，CI 使用 `pip-audit` 扫描已知漏洞，Dependabot 每周检查更新。升级 PyTorch、Ultralytics 或 OpenCV 时，必须重新运行模型加载和完整测试。
