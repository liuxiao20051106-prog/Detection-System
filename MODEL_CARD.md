# 模型卡：葡萄成熟度检测模型

## 基本信息

- 任务：目标检测
- 框架：Ultralytics YOLO
- 权重：`models/best.pt`
- SHA-256：`ada66cb1215507aa4652d60b073ef64f9c6465daeac7e96475abe151b3d120c0`
- 类别：`immature`、`semi-mature`、`mature`
- 输入：彩色图像或视频帧
- 输出：边界框、类别和置信度

## 现有评估记录

仓库保留的训练日志显示，最佳验证轮次为 85：

| 指标 | 值 |
| --- | ---: |
| Precision | 0.64801 |
| Recall | 0.59576 |
| mAP@0.5 | 0.61437 |
| mAP@0.5:0.95 | 0.45016 |

这些数值来自 `models/imported_train_result_20260516_220427/results.csv`。训练日志记录了 120 轮、640 输入尺寸、batch 128 和随机种子 0。

## 数据限制

训练数据集、标注规则、数据来源、授权文件、地域/品种/光照分布和训练集划分未包含在仓库中。因此：

- 无法仅根据本仓库独立复现训练和指标；
- 无法确认模型对不同品种、产地、季节、设备、遮挡和光照的泛化性；
- `TestFiles/` 只是界面演示素材，不是可引用的标注测试集。

## 适用与不适用场景

适合教学、原型演示和经人工复核的辅助检测。不应作为食品安全、采收决策、品质认证、采购结算或其他高影响决策的唯一依据。请在目标环境用独立标注数据评估，并保留人工复核。

## 更换模型

仅使用来源可信的模型。更换后同时设置 `DETECTION_MODEL_PATH` 和对应的 `DETECTION_MODEL_SHA256`，并确保三个类别的编号与 `Config.py` 完全一致。
