## 变更内容

- （请填写）

## 验证

- [ ] `python -m unittest discover -s tests -v`
- [ ] `python -m ruff check .`
- [ ] 修改依赖时已运行 `python -m pip_audit -r requirements.txt --strict`
- [ ] 修改默认模型时已更新哈希、模型卡和模型加载测试

## 风险与回退

- 风险：
- 回退方法：
